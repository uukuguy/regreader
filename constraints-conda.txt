# Constraints for conda environment installation
# Pin exact versions to prevent pip from rebuilding packages
# that require Rust/C++ compilers

# Check your conda versions with: pip list | grep -E "tiktoken|torch"
# Update these versions to match your conda environment
tiktoken==0.9.0
torch
torchvision
torchaudio
